---
import ThemeToggle from "./ThemeToggle.astro";

const currentPath = Astro.url.pathname;

const isActive = (href: string) => {
  if (href === "/" && currentPath === "/") return true;
  if (href !== "/" && currentPath.startsWith(href)) return true;
  return false;
};
---

<header class="sticky top-0 z-50 backdrop-blur-sm bg-[var(--ctp-mantle)]/95 border-b border-[var(--ctp-surface)]">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center space-x-2 group">
      <div class="flex space-x-1">
        <div class="w-6 h-6 bg-[var(--ctp-text)] rounded-sm transform group-hover:scale-110 transition-transform"></div>
        <div class="w-6 h-6 bg-[var(--ctp-accent)] rounded-sm transform group-hover:scale-110 transition-transform"></div>
      </div>
      <span class="font-bold text-lg text-[var(--ctp-text)]">Andre Scutieri</span>
    </a>

    <!-- Navigation + Theme Toggle -->
    <div class="flex items-center space-x-8">
      <a
        href="/"
        class={`transition-colors ${
          isActive("/")
            ? "text-[var(--ctp-text)] font-semibold"
            : "text-[var(--ctp-subtext)] hover:text-[var(--ctp-text)]"
        }`}
      >
        home
      </a>
      <a
        href="/sobre"
        class={`transition-colors ${
          isActive("/sobre")
            ? "text-[var(--ctp-text)] font-semibold"
            : "text-[var(--ctp-subtext)] hover:text-[var(--ctp-text)]"
        }`}
      >
        sobre
      </a>
      <a
        href="/blog"
        class={`transition-colors ${
          isActive("/blog")
            ? "text-[var(--ctp-text)] font-semibold"
            : "text-[var(--ctp-subtext)] hover:text-[var(--ctp-text)]"
        }`}
      >
        blog
      </a>

      <div class="w-px h-6 bg-[var(--ctp-surface)]"></div>

      <ThemeToggle />
    </div>
  </nav>
</header>
