---
title: "Progressive Web Apps: Offline First"
description: "Building Progressive Web Apps that work offline with service workers and caching."
pubDate: 2025-01-25
---

# Progressive Web Apps: Offline First

PWAs combine the best of web and native apps. Let's build one.

## Service Worker

```javascript
self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open('v1').then((cache) => {
      return cache.addAll([
        '/',
        '/styles/main.css',
        '/scripts/app.js'
      ]);
    })
  );
});

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request)
      .then((response) => response || fetch(event.request))
  );
});
```

## Web App Manifest

```json
{
  "name": "My App",
  "short_name": "MyApp",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#000000",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    }
  ]
}
```

## Key Features

| Feature | Benefit |
|---------|---------|
| Offline support | Works without internet |
| Installable | Add to home screen |
| Push notifications | Re-engagement |
| Fast loading | Service worker cache |

---

PWAs: Web apps that feel native!
